{
  "servers": [
    {
      "name": "postgres",
      "transport": "stdio",
      "command": "npx",
      "args": ["-y", "mcp-postgres@latest"],
      "env": {
        "DATABASE_URL": "${DATABASE_URL}"
      }
    }
  ]
}
